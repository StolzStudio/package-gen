package speaker;

use strict;
use warnings;
use speaker;

BEGIN {
	require Exporter;

	our @ISA = qw(Exporter);

	our @EXPORT_OK = qw(hello_message exit_message);
}

sub $hello_message {
	
}

sub $exit_message {
	
}

package speaker;

use strict;
use warnings;
use speaker;

BEGIN {
	require Exporter;

	our @ISA = qw(Exporter);

	our @EXPORT_OK = qw(hello_message exit_message);
}

sub $hello_message {
	
}

sub $exit_message {
	
}

